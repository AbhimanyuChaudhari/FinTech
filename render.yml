services:
  - type: web
    name: ai-fund-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "./start.sh"
    workingDir: backend  # or '.' if main.py is in the root